import type { CSSProperties } from "react";

export const quizStyles: Record<string, CSSProperties> = {
  page: {
    minHeight: "100vh",
    background:
      "linear-gradient(180deg, #0B0E1A 0%, #1A2332 40%, #2D4A1E 80%, #3D6B24 100%)",
    display: "flex",
    alignItems: "flex-start",
    justifyContent: "center",
    padding: "24px 16px",
    fontFamily: "'Nunito', sans-serif",
    position: "relative",
  },
  card: {
    background:
      "linear-gradient(170deg, rgba(30,40,55,0.95) 0%, rgba(20,28,40,0.98) 100%)",
    maxWidth: 520,
    width: "100%",
    borderRadius: 16,
    padding: "24px 24px 20px",
    border: "1px solid rgba(255,255,255,0.08)",
    boxShadow:
      "0 20px 60px rgba(0,0,0,0.5), inset 0 1px 0 rgba(255,255,255,0.05)",
    position: "relative",
    zIndex: 1,
  },
  header: {
    display: "flex",
    justifyContent: "space-between",
    alignItems: "center",
    marginBottom: 16,
    paddingBottom: 12,
    borderBottom: "1px solid rgba(255,255,255,0.06)",
  },
  headerTitle: {
    fontFamily: "'Silkscreen', monospace",
    fontSize: 15,
    color: "#A0D468",
    letterSpacing: 1,
  },
  scoreBadge: {
    display: "flex",
    alignItems: "center",
    gap: 6,
    padding: "4px 12px",
    background: "rgba(255,215,0,0.12)",
    border: "1px solid rgba(255,215,0,0.25)",
    borderRadius: 20,
    color: "#FFD700",
    fontWeight: 800,
    fontSize: 14,
    fontFamily: "monospace",
  },
  questionArea: {
    marginBottom: 20,
  },
  topicBadge: {
    display: "inline-flex",
    alignItems: "center",
    gap: 6,
    padding: "4px 12px",
    background: "rgba(124,252,0,0.08)",
    border: "1px solid rgba(124,252,0,0.2)",
    borderRadius: 20,
    color: "#A0D468",
    fontSize: 12,
    fontWeight: 700,
    letterSpacing: 0.5,
  },
  questionText: {
    fontSize: 17,
    fontWeight: 700,
    color: "#E8F5E9",
    lineHeight: 2,
    margin: 0,
  },
  questionSub: {
    fontSize: 14,
    color: "#81C784",
    marginTop: 6,
    fontStyle: "italic",
    opacity: 0.8,
  },
  optionBtn: {
    display: "flex",
    alignItems: "center",
    gap: 12,
    width: "100%",
    padding: "14px 16px",
    fontSize: 16,
    fontWeight: 700,
    fontFamily: "'Nunito', sans-serif",
    cursor: "pointer",
    background: "rgba(255,255,255,0.06)",
    border: "1px solid rgba(255,255,255,0.12)",
    borderRadius: 10,
    color: "#E0E0E0",
    transition: "all 0.2s ease",
    textAlign: "left" as const,
  },
  optionLetter: {
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    width: 28,
    height: 28,
    borderRadius: 6,
    background: "rgba(255,255,255,0.08)",
    color: "#A0D468",
    fontSize: 13,
    fontWeight: 800,
    fontFamily: "monospace",
    flexShrink: 0,
  },
  hintBtn: {
    padding: "10px 20px",
    fontSize: 14,
    fontWeight: 700,
    fontFamily: "'Nunito', sans-serif",
    cursor: "pointer",
    background: "rgba(255,213,79,0.1)",
    border: "1px solid rgba(255,213,79,0.3)",
    borderRadius: 8,
    color: "#FFD54F",
    transition: "all 0.2s",
  },
  hintsBox: {
    display: "flex",
    flexDirection: "column",
    gap: 8,
    marginBottom: 12,
    padding: 12,
    background: "rgba(255,213,79,0.05)",
    borderRadius: 10,
    border: "1px solid rgba(255,213,79,0.1)",
  },
  hintItem: {
    display: "flex",
    alignItems: "flex-start",
    fontSize: 14,
    color: "#E0E0E0",
    lineHeight: 1.5,
  },
  footer: {
    marginTop: 20,
    textAlign: "center" as const,
    fontSize: 12,
    color: "#666",
    fontFamily: "monospace",
    letterSpacing: 1,
  },
  title: {
    fontFamily: "'Silkscreen', monospace",
    fontSize: 28,
    color: "#7CFC00",
    marginBottom: 8,
    letterSpacing: 2,
    textAlign: "center" as const,
  },
  restartBtn: {
    padding: "12px 28px",
    fontSize: 16,
    fontWeight: 700,
    fontFamily: "'Nunito', sans-serif",
    cursor: "pointer",
    background: "linear-gradient(135deg, #43A047, #2E7D32)",
    border: "2px solid #1B5E20",
    borderRadius: 10,
    color: "#fff",
    boxShadow: "0 4px 12px rgba(46,125,50,0.4)",
  },
};

export const keyframes = `@import url('https://fonts.googleapis.com/css2?family=Silkscreen:wght@400;700&family=Nunito:wght@600;700;800&display=swap');
@keyframes floatUp { 0% { transform: translateY(0) rotate(0deg); opacity: 0; } 10% { opacity: 0.6; } 90% { opacity: 0.3; } 100% { transform: translateY(-100vh) rotate(360deg); opacity: 0; } }
@keyframes popIn { 0% { transform: scale(0.8); opacity: 0; } 100% { transform: scale(1); opacity: 1; } }
@keyframes headShake { 0% { transform: translateX(0); } 15% { transform: translateX(-8px); } 30% { transform: translateX(6px); } 45% { transform: translateX(-4px); } 60% { transform: translateX(2px); } 100% { transform: translateX(0); } }
@keyframes slideIn { 0% { transform: translateX(-12px); opacity: 0; } 100% { transform: translateX(0); opacity: 1; } }
@keyframes bounce { 0%, 100% { transform: translateY(0); } 50% { transform: translateY(-10px); } }`;
